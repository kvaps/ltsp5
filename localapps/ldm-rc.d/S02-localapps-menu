# Generate the localapp menu

if boolean_is_true "$LOCAL_APPS" -a boolean_is_true "$LOCAL_APPS_MENU"; then
    [ -x "/usr/bin/ltsp-genmenu" ] && su - ${LDM_USERNAME} -c "/usr/bin/ltsp-genmenu install"
fi
