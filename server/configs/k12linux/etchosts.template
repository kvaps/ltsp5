ipaddr=$(ip addr show to 0/0 scope global | awk '/[[:space:]]inet / { print gensub("/.*","","g",$2) }')
echo "# Generated by client during bootup" >> /etc/hosts
echo "127.0.0.1 localhost"      >> /etc/hosts
echo "$ipaddr	client-$ipaddr" >> /etc/hosts
