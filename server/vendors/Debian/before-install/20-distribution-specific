case "$DIST" in
     sid)
	    echo "NOTE: disabling security APT source for sid.  It does not exist."
	    NOSECURITY=true
	    ;;
     sarge)
	    EARLY_PACKAGES="$EARLY_PACKAGES devfsd"
	    ;;
     *)
esac
