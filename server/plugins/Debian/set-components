case "$MODE" in
    commandline)
	    add_option "components" "Set the components to be used in mirror" "advanced" "true"
	    ;;
    configure)
	    if [ -n "$option_components_value" ]; then
		COMPONENTS=$option_components_value
	    fi
	    ;;
esac
