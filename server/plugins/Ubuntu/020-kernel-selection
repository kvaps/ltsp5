case "$MODE" in
    before-install)
	case $ARCH in
	    i386)
		KERNEL_ARCH="386"
		;;
	    amd64)
		KERNEL_ARCH="amd64-generic"
		;;
	    sparc) 
		KERNEL_ARCH="$(uname -m)"
		;;
	    *)
		KERNEL_ARCH="$ARCH"
		;;
	esac
	LATE_PACKAGES="$LATE_PACKAGES linux-image-$KERNEL_ARCH"
	;;
esac
