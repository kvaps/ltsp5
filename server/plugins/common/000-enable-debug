case "$MODE" in
    commandline)
            add_option "debug" "Enable debug support" "advanced" "false"
            ;;
    configure)
            if [ -n "$option_debug_value" ]; then
                DEBUG=true # Set on configure to have it set for all plugins
            fi
            ;;
esac
