case "$MODE" in
    configure)
        case "$ARCH" in
            alpha|amd64|arm|i386|ia64)
                EARLY_PACKAGES="$EARLY_PACKAGES joystick"
                ;;
        esac
        ;;
esac
