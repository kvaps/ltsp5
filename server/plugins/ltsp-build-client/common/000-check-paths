case "$MODE" in
    configure)
        unset CHROOTEXEC
        for x in /usr/sbin/chroot /usr/bin/chroot; do
            [ -e $x ] && CHROOTEXEC=$x
        done
        if [ -z "$CHROOTEXEC" ]; then
            echo "ERROR: chroot command not found!"
            exit 1
        fi
        ;;
esac

