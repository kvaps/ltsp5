#!/bin/sh

PREREQ=""

prereqs()
{
	echo "$PREREQ"
}

case $1 in
# get pre-requisites
prereqs)
	prereqs
	exit 0
	;;
esac

mkdir /cow
mount -t tmpfs tmpfs /cow
mount -t unionfs -o delete=whiteout,dirs=/cow=rw:${rootmnt}=nfsro unionfs ${rootmnt}
